# OpenRouter API Key for AI-powered changelog and version detection
# Get your API key from: https://openrouter.ai/keys
# Leave empty to use conventional commits fallback
OPENROUTER_API_KEY=your_openrouter_api_key_here

# GitHub Token (optional, for local release creation)
# If not set, GitHub Actions will handle releases automatically
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo (for creating releases)
GITHUB_TOKEN=your_github_token_here

# Git remote name (default: origin)
GIT_REMOTE=origin

# Git branch name (default: master)
GIT_BRANCH=master
